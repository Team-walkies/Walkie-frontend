import React from "react";
import { Route, Routes } from "react-router-dom";

const MapPages = () => {
  return (
    <div>
      <Routes>
        <Route path="/" />
      </Routes>
    </div>
  );
};

export default MapPages;
